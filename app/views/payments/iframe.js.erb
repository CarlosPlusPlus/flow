// TODO: Add HTML + Price of FLOW [taken from span].
// TODO: Add HTML element for new content.

// Add iFrame to pre-selected div on checkout page.
$("#payflow-iframe").html("<%= raw(@iframe) %>");

// Scroll down to iframe once DOM has been updated.
$('html,body').animate({
  scrollTop: $("#payflow-iframe").offset().top - 225 }, 1000);
